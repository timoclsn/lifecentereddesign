import * as categories from '../data/categories/action';
import * as newsletter from '../data/newsletter/action';
import * as resources from '../data/resources/action';
import * as topics from '../data/topics/action';
import * as types from '../data/types/action';
import * as users from '../data/users/action';

export const action = {
  categories,
  newsletter,
  resources,
  topics,
  types,
  users,
};
