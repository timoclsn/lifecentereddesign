import 'server-only';
import * as collections from '../data/collections/action';
import * as resources from '../data/resources/action';
import * as users from '../data/users/action';

export const action = {
  collections,
  resources,
  users,
};
